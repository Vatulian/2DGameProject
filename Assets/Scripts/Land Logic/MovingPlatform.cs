using UnityEngine;

public class MovingPlatform : MonoBehaviour
{
    // Marker component for moving platform prefabs.
    // Player carry is handled by PlayerMovement so Rigidbody2D motion has one owner.
}
